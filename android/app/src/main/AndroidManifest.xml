<?xml version="1.0" encoding="utf-8"?>
<!--
  Permissions per the plan: INTERNET for the rippers themselves, the two FOREGROUND_SERVICE
  entries for RipService (foregroundServiceType="dataSync" - a bulk download is the textbook case
  for that type), POST_NOTIFICATIONS so the progress notification is visible on API 33+ (the
  service still runs and keeps the process alive without it - MainActivity's runtime request is
  best-effort, not a gate).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".RipMeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.RipMe">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Started by RipEngine (see its onRipStarted hook) when the queue starts draining,
             stops itself once RipService's own state collector observes the queue is idle again. -->
        <service
            android:name=".service.RipService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

    </application>

</manifest>
